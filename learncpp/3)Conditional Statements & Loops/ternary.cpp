#include<bits/stdc++.h>
using namespace std;
int main() {
    int n = 5;

    cout << (n>=0 ? "Positive" : "Negative") << endl; // Ternary operator usage
    cout << (n%2 == 0 ? "Even" : "Odd") << endl; // Ternary operator usage  
    return 0;
}